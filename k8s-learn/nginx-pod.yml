
# api version
apiVersion: v1

# the kind to configure
kind: Pod
metadata:
  name: "nginx-app4"
spec:
  containers:
    - name: nginx-app
      image: nginx:latest
      ports: 
        - containerPort: 80